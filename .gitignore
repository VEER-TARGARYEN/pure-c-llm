# ---- build artifacts ----
*.exe
*.o
*.obj
*.out
train_gpu

# ---- vendored dependency (downloaded, not source) ----
openblas/
openblas.zip

# ---- large model checkpoints (published via GitHub Releases / Kaggle) ----
checkpoint_latest.bin
ckpt_*.bin
model_ckp1.bin
model_weights.bin

# ---- local-only documentation (deliberately NOT published) ----
RESEARCH_AND_METHODS.md
INTERVIEW_PREP.md

# ---- logs / scratch ----
*.log
sample.txt
corpus.txt

# ---- interview / video prep (local only) ----
demo/
